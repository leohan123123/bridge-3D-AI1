fastapi
uvicorn[standard] # Includes pydantic, starlette, etc.
# three.js is a frontend library, included via CDN in HTML.
# No explicit Python dependencies for the core generation logic other than standard library.
