fastapi>=0.100.0 # Using a recent version, can be adjusted
uvicorn[standard]>=0.20.0 # Includes standard dependencies like websockets, http-tools
pydantic>=2.0.0 # For data models
httpx>=0.25.0 # For making async HTTP requests in llm_service
